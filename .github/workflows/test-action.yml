name: Node CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Hello world
      uses: actions/some-test-action-12345@v1.0.2
      with:
        who-to-greet: 'Mona the Octocat'
